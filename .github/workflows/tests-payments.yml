name: Tests-Payments

on:
  pull_request:
    paths:
      - 'payments/**'
  #   branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: cd payments && npm install && npm run test:ci
